<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.core.data_02">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <!--  -->
        <!-- Main Activity -->
        <!--  -->
        <activity
                android:name="com.core.data_02.MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity> <!--  -->
        <!--  -->
        <!-- Preference Activities -->
        <!--  -->
        <activity
                android:name="com.core.data_02.PrefsActivity"
                android:label="@string/title_activity_prefs"
                android:parentActivityName="com.core.data_02.MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.core.data_02.MainActivity" />
        </activity><!--     -->
        <!--  -->
        <!-- Detail Activities -->
        <!--  -->
        <activity
                android:name="com.core.data_02.DetailActivity"
                android:label="@string/vessel_details"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>

    </application>

</manifest>